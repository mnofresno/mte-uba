<%= navigation(:id => "main-navigation") do |menu| %>
  <%# if current_user.can_operated_engine?(:MteUba) %>

    <% menu.item(Unidad.model_name.human.pluralize, unidades_path, :active => controller.controller_name.include?('unidades')) %>
    <% menu.item(Proveedor.model_name.human.pluralize, proveedores_path, :active => controller.controller_name.include?('proveedores')) %>
    <% menu.item('Nota Pedido', root_path, :active => controller.controller_name.include?('unidades')) %>
    <% menu.item('Nota Reparacion', root_path, :active => controller.controller_name.include?('unidades')) %>
    <%# menu.item(NotaPedido.model_name.human, notapedido_path, :active => controller.controller_name.include?('notapedio')) %>
    <%# menu.item(NotaReparacion.model_name.human.pluralize, notareparacion_path, :active => controller.controller_name.include?('notareparacion')) %>
  <%# end %>

<% end %>
