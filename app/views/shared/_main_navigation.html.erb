<%= navigation(:id => "main-navigation") do |menu| %>
  <%# if current_user.can_operated_engine?(:MteUba) %>

    <% menu.item(Unidad.model_name.human.pluralize, unidads_path, :active => controller.controller_name.include?('unidads')) %>
    <% menu.item(Proveedor.model_name.human.pluralize, proveedors_path, :active => controller.controller_name.include?('proveedors')) %>
    <% menu.item('Nota Pedido', root_path, :active => controller.controller_name.include?('unidads')) %>
    <% menu.item('Nota Reparacion', root_path, :active => controller.controller_name.include?('unidads')) %>
    <%# menu.item(NotaPedido.model_name.human, notapedido_path, :active => controller.controller_name.include?('notapedio')) %>
    <%# menu.item(NotaReparacion.model_name.human.pluralize, notareparacion_path, :active => controller.controller_name.include?('notareparacion')) %>
  <%# end %>

<% end %>
