<%= form_for(@choferes_unidades) do |f| %>
  <% if @choferes_unidades.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@choferes_unidades.errors.count, "error") %> prohibited this choferes_unidades from being saved:</h2>

      <ul>
      <% @choferes_unidades.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :belongs_to %><br>
    <%= f.text_field :belongs_to %>
  </div>
  <div class="field">
    <%= f.label :belongs_to %><br>
    <%= f.text_field :belongs_to %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
