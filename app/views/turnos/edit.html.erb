<% content_for(:breadcrumbs) do %>
  <%= breadcrumbs do |b|
    b.item Turno.model_name.human, turnos_path
    b.item @turno.descripcion, root_path, :active => true
  end %>
<%end%>

<%= render 'form' %>
