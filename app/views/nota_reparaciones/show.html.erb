<%- model_class = NotaReparacion -%>
<div class="page-header">
  <h1><%=t '.title', :default => model_class.model_name.human.titleize %></h1>
</div>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:numeroserie) %>:</strong></dt>
  <dd><%= @nota_reparacion.numeroserie %></dd>
  <dt><strong><%= model_class.human_attribute_name(:fecha) %>:</strong></dt>
  <dd><%= @nota_reparacion.fecha %></dd>
  <dt><strong><%= model_class.human_attribute_name(:detalles) %>:</strong></dt>
</dl>

<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:cantidad) %></th>
      <th><%= model_class.human_attribute_name(:descripcion) %></th>
    </tr>
  </thead>
  <tbody>
    <% @nota_reparacion.detalles do |detalle| %>
      <tr>
        <td><%= detalle.cantidad %></td>
        <td><%= detalle.descrip %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.back', :default => t("helpers.links.back")),
              nota_reparaciones_path, :class => 'btn btn-default'  %>
<%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_nota_reparacion_path(@nota_reparacion), :class => 'btn btn-default' %>
<%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              nota_reparacion_path(@nota_reparacion),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
